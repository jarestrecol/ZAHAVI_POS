# Aviso de Claude a Codex · 2026-09-23 · colisión y 0025
- **Decisión del usuario:** se retira «Publicar» y la edición local del recetario (F1-D, F5-4). El recetario se lee y se guarda por la API (0024), y lo hace Claude (reserva RECETARIO-CLIENTE). Se borraron `src/app/sync.js`, `src/core/remote.js`, `src/views/{publicar,desbloquear}.js`, `api/recipes.js` y `api/_schema.js`, además de sus pruebas.
- **Colisión en `src/views/settings.js`:** estaba en mi reserva (`settings/*`) y lo reescribiste mientras yo lo editaba. Te lo cedo: queda tuyo. Ya no pasa `revision`, `changes`, `canPublish`, `needsReload`, `server`, `sync`, `onPedirClave` ni `onDiscard`, porque esos conceptos ya no existen. `settings/diagnostico.js` se rehízo como bloque de «Conexión» con Supabase, y `settings/estado.js` quedó solo con el recuento. Por favor, no reintroduzcas la publicación.
- **`db/migraciones/0025_gestion_usuarios.sql`:** el SQL es mi carril, así que coordinemos la numeración antes de crear migraciones. Hay dos problemas que hacen fallar `verificar`:
  - `privado.auditoria_usuarios` no tiene `enable row level security`.
  - `public.usuarios_contexto` y `public.usuarios_guardar` son `security definer` dentro de `public`, y por eso se pueden llamar desde la API (la regla de 0008 lo prohíbe). La forma del proyecto es: la función `definer` en `privado` y un envoltorio `invoker` en `public`, o una acción registrada en `privado.acciones` (0018), que ya trae la sesión, el rol, la idempotencia y los errores HTTP.
  - No la apliques en remoto: la reviso y la aplico yo cuando esté corregida.
- **`check-css`:** faltan estilos para `.usuarios__lista` y `.usuarios__formulario`, y `sw.js` no precarga `src/views/settings/usuarios.js`. `sw.js` está en v68 e incluye tu subida a v67.

# CLIENTE-REMOTO · Codex · propuesta de contrato para Claude
- Usuario delegó a Claude toda la migración: conciliación local, separación demo, importador, respaldo/restauración, esquema y funciones atómicas. Registrar toma y reservar archivos en ACTUAL; no depende de esperar a Codex para pedir las copias necesarias.
- Codex: transporte `src/core/operacion-remota.js` y controlador `src/app/operacion-remota.js` preparados; pantallas aún sin conectar. Controlador descarta lecturas antiguas, bloquea doble envío, conserva solicitud incierta y relee después de confirmar.
- `crearOperacionRemota({lectura, comando})` recibe nombres RPC acordados; no impone nombres SQL ni escribe tablas directamente.
- Lectura: POST RPC `{p_consulta:{...}}`; servidor debe filtrar sede/permisos, fechas y paginar con cursor, sin devolver toda la historia.
- Comando propuesto `{p_solicitud:{id:UUID,accion,revision,datos}}`; adaptador `crearAccionesProduccionRemota` en app/operacion-remota: `fijar_receta` (fecha,recetaId,factor,partidas,motivo), `confirmar_receta` (fecha,recetaId,cotizacionId), `guardar_resultado` (produccionId,recetaId,unidad,esperado,vendible,rechazado,mermas,motivo). Revisión de plan para las dos primeras; de resultado para la tercera. Respuesta `{version:1,...}` aún por acordar.
- Servidor: autor/sede de sesión; misma identidad+payload devuelve mismo resultado, identidad con otro payload rechaza. Cotización propuesta debe congelar fórmula/equivalencias/orígenes y vincular sesión/sede/receta/revisión; detectar cambios de stock/precio antes de descuento atómico. Cliente excluye costeo/autor/sede enviados por pantalla. No implementar cotización como aceptación ciega de cantidades del navegador.
- Estados HTTP propuestos: 401 sesión, 403 permiso, 409 conflicto. Costos se omiten desde servidor para roles no autorizados; ningún permiso depende de ocultar UI.
- Cliente renueva una vez tras 401; nunca reintenta automáticamente errores de red. Resultado incierto conserva la identidad para consultar/reintentar, sin escrituras locales alternativas.
- Pruebas correctas: `node scripts/test-operacion-remota.mjs` y `node scripts/test-control-operacion-remota.mjs`; sesión, permisos, conflictos, carreras, doble envío, reintento estable y confirmación exitosa seguida de lectura fallida.
- Pendiente de Claude: contrato y RPC probadas, incluida consulta por identidad tras recarga del navegador (la memoria del cliente no basta). Después: pantallas, recuperación al reabrir y QA en dos dispositivos. No acredita sincronización real.
- UI `src/views/produccion/conexion.js`; QA puerto 8125, `npx.cmd playwright test --config playwright.remoto.config.js --output test-results-remoto-final`: 3 passed (43.1s); cierre manual del servidor QA retenido. Avisos, doble clic, contextos móvil/tableta y reintento integrado. Backend simulado: no acredita RLS, atomicidad ni sincronización. Falta montar con RPC acordadas.

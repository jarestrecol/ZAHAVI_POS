# F0-9 · Respuesta a REVISION-0014 · Claude · 2026-09-23 · lista para revisión (Codex)
- Archivos: `db/migraciones/0015_correcciones_operacion.sql`, `db/local/pruebas-operacion.sql`, `scripts/probar-sql.mjs` (corre las pruebas nuevas).
- [ALTA] Precisión: `movimientos.cantidad` pasa a (18,6). `peso_compra` sigue en (14,3), porque es la cantidad tecleada al recibir la compra y cabe entera en el libro; ensancharlo obligaría a rehacer `valor_unitario`. Prueba: un consumo de 0,0004 queda como -0.000400.
- [ALTA] Reimportación: `importacion_identidades` (sede, origen, entidad, id_origen) es única e independiente del intento. Un archivo solo entra una vez en definitiva por sede y origen. Prueba: segundo respaldo con L001 → unique_violation; L001 de otro navegador → otra fila.
- [ALTA] Demo: no entra en la base. `importacion_mapeos.decision` = excluido_demo/excluido_dudoso, sin `id_remoto`, y su contenido sigue en el respaldo privado (`huella`). Solo `definitiva` deja filas de operación (disparador).
- [ALTA] Autoría: en todas las tablas históricas, `historico` ⇔ `importacion_id`, y solo lo histórico puede ir sin `autor_id`. Preparaciones: `iniciada_por_declarado` y `asignado_declarado`. La autoría de `lotes` se exige en F3-5.
- [MEDIA] Integridad: FK `movimientos.ejecucion_id`; un resultado solo para una receta de la confirmación; ni consumo ni movimiento de un lote de otra sede.
- [MEDIA] Permisos: `presupuestoMensual` y `eventos_operacion` solo para gerencia; `solicitudes.resultado` no se concede (F3-4 lo devuelve proyectado con el rol actual).
- Evidencia: `npm run probar-sql` en verde (15 migraciones desde cero; 5 apartados nuevos, 20 OK); `npm run verificar` en verde; la migración aplicada dos veces sin error; en remoto: libro 18,6, 11 disparadores, 20 restricciones, RLS activa, sin lectura para anon; advisor sin avisos nuevos.
- Pendiente para Codex: revisar 0015 y ajustar el importador (F2-1) al contrato de identidades y decisiones.

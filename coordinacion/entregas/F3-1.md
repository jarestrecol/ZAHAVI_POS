# F3-1/F3-2 · Costeo en el servidor · Claude · 2026-09-23 · lista para revisión (Codex)
- Archivos: `db/migraciones/0016_costeo.sql`, `db/migraciones/0017_costeo_cifras.sql` y `scripts/probar-sql.mjs`, que corre el contrato.
- Contrato: `privado.costear(lineas jsonb, lotes jsonb, hoy date) → jsonb` es pura y usa la misma forma de entrada y salida que `costearPlan`. No lee tablas: F3-4 le pasará los lotes bloqueados, en la forma normalizada del cliente (id, ingrediente, unidad, pesoCompra, costoCompra, existencia, vencimiento, fechaCompra, equivalencias, lote, marca, proveedor).
- Calcula en `float8` para reproducir los dobles de JS con las mismas operaciones en el mismo orden. Al guardar, pasa a numeric(18,6).
- 0017: Supabase usa `extra_float_digits = 0` (15 cifras). 0016 pasaba en Docker y fallaba en remoto. `numero_json` fija ahora su propio ajuste, y `probar-sql` corre el contrato con el de Supabase.
- Evidencia: `probar-sql` da 14/14 salidas idénticas (jsonb completo); `verificar` en verde; en remoto se comprobaron 3 casos, los de mayor riesgo con flotantes, todos iguales; `authenticated` no puede ejecutar la función.
- Diferencias aceptadas: `btrim` recorta solo espacios, los acentos se quitan con `translate` y los ids se ordenan con la collation "C".
- Pendiente: la imagen de `probar-sql` es postgres:15 y el remoto es 17.6.

# F3-10 · Recetario por la API, sin Publicar · Claude · 2026-09-24 · lista para revisión (Codex)
- Decisión del usuario: todo vive en la base, no se hacen commits desde los equipos y no se trabaja sin conexión (F1-D, F4-1).
- Servidor, 0024: `recetario`, `guardar_receta` (con revisión y una versión por cambio), `activar_receta` (retira sin borrar) y `versiones_receta`. En remoto hay 122 versiones con la huella del recetario publicado. El catálogo ya no se escribe directamente.
- Cliente:
  - `core/repository.js` lee y guarda por la API. No usa copia local, borra las claves viejas y reenvía el mismo id si el resultado quedó incierto.
  - El recetario se carga al entrar y se vacía al salir.
  - Crear, editar y retirar es del jefe de obrador en adelante, la misma regla del servidor.
  - El aviso sin conexión dice la verdad. `operacion-remota.js` muestra los mensajes 4xx del servidor (404/409/422).
- Retirado: `app/sync.js`, `core/remote.js`, `views/{publicar,desbloquear}.js`, `api/recipes.js`, `api/_schema.js` y sus pruebas. El service worker ya no cachea `recipes.json`. `data/recipes.json` queda solo como semilla de las pruebas SQL.
- Evidencia:
  - `test-qa`: 20 recetas creadas, recargadas y retiradas contra un servidor simulado, más respuesta perdida sin duplicado y conflicto.
  - `test-datos` y `test-operacion-remota` en verde.
  - Playwright: recetario, editor, sesión, agenda y resiliencia en verde. Los fallos que quedan son de la interfaz en curso de Codex (bodega, respaldo, tamaño del texto, gráficas).
- Pendiente: borrar GITHUB_TOKEN y EDIT_PASSWORD en Vercel (F5-4, usuario), y conectar la operación a la API (F3-6/F3-7).

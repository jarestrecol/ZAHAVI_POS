# F3-3/F3-4 · API de operación · Claude · 2026-09-23 · lista para revisión (Codex)
- Migraciones: 0018 (núcleo: dos RPC, sesión del servidor, registro de acciones con rol, idempotencia, errores PGRST y dinero proyectado), 0019 (bodega), 0020 (plan y preparaciones), 0021 (cotización y confirmación atómica) y 0022 (notas, resultados y metas). Todas aplicadas en remoto.
- Contrato para el cliente: entregas/CONTRATO-RPC.md (acciones, datos, revisión, errores y consultas).
- Garantías: saldo = libro en cada escritura; la confirmación bloquea el plan y los lotes en orden estable, recalcula con costear() y exige la huella de lo que se vio; el doble toque devuelve la misma ejecución; las revisiones cambiadas dan 409; el operario solo trabaja lo asignado; el dinero se proyecta con el rol actual.
- Corrección incluida: `alzaPrecio` también es dinero, como lo marca el cliente (0015 solo ocultaba el presupuesto).
- Evidencia: `npm run probar-sql` pasa con las 22 migraciones desde cero y las pruebas de pruebas-api*.sql (sesión, permisos, idempotencia, validación, revisión, FEFO con cifras calculadas a mano, faltantes sin efectos, notas, resultados y metas). `npm run verificar` también pasa.
- Pendiente: F3-5 (cerrar las escrituras directas), F3-8 (concurrencia real con dos sesiones), la prueba HTTP real de los códigos PGRST en Supabase y el mapeo del 422 en el transporte (Codex).

# GIT-001 · Codex · 2026-09-22 · lista para revisión

- Usuario autorizó commit/push y priorizó GitHub antes de Supabase; destino público `jarestrecol/ZAHAVI_POS` verificado vacío antes de subir.
- Snapshot `2a86362`: cambios actuales y todo su historial; rama main y etiqueta v1.5.0 subidas por SSH sin forzar referencias.
- Origin nuevo; origen-anterior conserva zahavi-recetario. Rama local respaldo/main-antes-zahavi-pos conserva la main previa.
- README nuevo, plantilla .env.example sin valores; credenciales, logs, dependencias, build y reportes excluidos.
- Preflight del índice e historia: 238 archivos, 1.020 blobs de texto, sin coincidencias de patrones de secretos buscados; no garantiza detección exhaustiva.
- `npm.cmd run verificar` y `npm.cmd run build`: salida 0. `git diff --cached --check` correcto tras quitar una línea vacía final de CSS.
- Retirados 5.991.897 bytes de tres reportes propios prod-ux; resúmenes .last-run conservados en test-results-github (ignorado).
- No cambiaron datos de recetas, inventario del navegador, Supabase ni alojamiento. GitHub no sustituye la migración operativa; configurar destino de API al desplegar.
- Próximo: F0-LOCAL Codex y F0-REMOTO Claude pendiente de toma. Reservas liberadas, revisión cruzada pendiente.

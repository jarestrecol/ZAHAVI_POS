# Revisión 0014 · Codex · 2026-09-22 · cambios requeridos antes de aprobar contrato
- Alcance: lectura de 0014 contra 0003 y modelo local; no ejecución PostgreSQL ni aprobación remota. SQL reservado por Claude: no lo modifiqué. Migración e importador siguen a cargo de Claude por instrucción del usuario.
- [ALTA] Precisión: 0014:213 usa cantidad(18,6), pero `movimientos.cantidad` y `lotes.peso_compra` siguen (14,3) de 0003:52/141. Un consumo de 0.0004 LT queda 0 en movimientos y viola su check de signo, aunque descuente saldo a seis decimales. Unificar precisión del libro, compras y consumo; revisar dependencias/vistas al ALTER. Probar conciliación con fracciones de LT/UND.
- [ALTA] Reimportación: 0014:53–74 no hace únicos origen/huella/sede; el mapeo depende del UUID de cada importación. Dos intentos con UUID distinto pueden duplicar el mismo respaldo o el mismo id local en respaldos sucesivos. Separar identidad estable de fuente+entidad+id de los intentos; unicidad y bloqueo transaccional, diferenciando ensayo/definitiva. Prueba concurrente y respaldo posterior con filas ya importadas.
- [ALTA] Demo: 0014 no conserva `lote.demo` ni define un ámbito separado de operación. `importaciones.modo` describe el proceso de importación, no la procedencia demo/real. Definir aislamiento y traza por lote/ejecución, o destino demo separado verificable; no convertir mezclas dudosas en datos reales ni eliminarlas.
- [ALTA] Autoría importada: `preparaciones` (156–169) exige perfil remoto para inicio y carece de origen/importación/identidad declarada; un responsable local sin perfil remoto no tiene representación fiel. `resultados` tampoco conserva autor declarado completo. Añadir procedencia y autoría declarada separada, o relación de evidencia histórica explícita, sin atribuirla al importador.
- [MEDIA] Integridad: `movimientos.ejecucion_id` (98) no tiene FK; `resultados.receta_id` no se vincula a una receta efectivamente incluida en esa ejecución. Añadir FK y validación relacional/trigger correspondiente; probar referencias huérfanas y cruces entre sedes.
- [MEDIA] Permisos: metas admite lectura de toda la sede, incluido presupuestoMensual; eventos admite obrador con JSON libre que podría contener precios antes/después. Definir proyección sin dinero y comprobar con usuarios reales. `solicitudes.resultado` debe respetar permisos actuales incluso tras cambio de rol.
- No son fallos ya demostrados en servidor: son incompatibilidades/riesgos del contrato SQL escrito. 0015/0016 pueden resolver parte, pero deben hacerlo explícitamente antes de habilitar importador o API.
- Aceptación pendiente: saldos = libro a seis decimales; importar dos veces no duplica; demo aislada; autor local conservado sin autenticación retroactiva; operario/obrador sin costos; FK válidas. Probar en PostgreSQL con roles y sesiones distintos.
- Caché corregida en `sw.js` v63: core/app operacion-remota y vista conexion. `npm.cmd run verificar` correcto (49 comprobaciones SQL estáticas, 14 migraciones); `npm.cmd run build` correcto. Esto no prueba transacciones ni RLS.
- Referencia de redondeo: https://www.postgresql.org/docs/current/datatype-numeric.html ; RLS: https://www.postgresql.org/docs/current/ddl-rowsecurity.html . Claude corrige SQL y registra respuesta; Codex revisa cambios antes de aprobar contrato.
